import { HashLoader } from 'react-spinners'

const Loader = () => {
  return (
    <div className="loader__container">
      <HashLoader className="loader" color="#a9dcec" />
    </div>
  )
}

export default Loader
